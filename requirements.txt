# Hybrid Stock Forecasting - MSc AI Research Practicum Part 2
# Mukesh Mukkara (x24226432), National College of Ireland
#
# Install:  pip install -r requirements.txt
#
# Python 3.11 (developed and reported on 3.11.9). 3.12 also works.
# Versions are pinned to exactly what produced the reported results.
# torch here is CPU-only. For an NVIDIA GPU, install torch from https://pytorch.org instead.

# --- numerical / data ---
numpy==2.3.3
pandas==2.3.3

# --- the ML arm ---
scikit-learn==1.8.0          # StandardScaler, TF-IDF retrieval, metrics
keras==3.12.0                # LSTM
torch==2.12.0                # Keras backend, set in src/ml_model.py (no TensorFlow required)
xgboost==3.2.0               # one of the classical baselines in the section 2 model search

# --- charts ---
matplotlib==3.10.9

# --- data source ---
yfinance==1.3.0              # daily prices and the VIX

# --- the LLM arms ---
google-genai==1.75.0         # Gemini API client
python-dotenv==1.0.0         # reads GEMINI_API_KEY from .env (the key is never hard-coded)

# --- the web demo (app.py) ---
flask==3.1.3

# --- to open the research notebook ---
# `notebook` rather than the `jupyter` meta-package: the notebook uses no widgets,
# and the meta-package pulls ipywidgets, whose JupyterLab extension ships a 270-char
# path that fails to install on Windows unless long-path support is switched on.
ipykernel==6.30.1
notebook==7.6.1              # provides `jupyter notebook research.ipynb`
